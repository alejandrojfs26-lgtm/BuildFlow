apiVersion: 1

providers:
  - name: "BuildFlow Dashboards"
    orgId: 1
    folder: "BuildFlow"
    folderUid: "buildflow"
    type: file
    disableDeletion: true
    editable: false
    options:
      path: /var/lib/grafana/dashboards
