name: Deploy

on:
  workflow_run:
    workflows: [CI]
    types: [completed]
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    steps:
      - uses: actions/checkout@v4
      - name: Deploy to production
        run: |
          echo "Deployment placeholder — replace with SSH/Ansible/Kubernetes deploy"
          # Example SSH deploy:
          # ssh deploy@${{ secrets.DEPLOY_HOST }} "
          #   cd /opt/buildflow &&
          #   docker compose pull &&
          #   docker compose up -d --force-recreate
          # "
